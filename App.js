import Expo from 'expo';
import App from './src/main';

Expo.registerRootComponent(App);
// export default App; 
// src로 실행 소스파일들을 모음
// 프로젝트 구조 설계를 직접 함 
// package.json 파일의 main 설정을 expo가 아닌 이 파일로 변경
